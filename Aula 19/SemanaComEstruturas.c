#include<stdio.h>

enum e_semana{domingo=1, segunda, terca, quarta, quinta, sexta, sabado};

int main(int argc, char *argv[]){
    printf("Semana = %d %d %d \n", domingo, terca, sabado);
    return 0;
}
